//template for building objects, e.g. drone, queen, worker, etc.

function Person(name){
  this.name = name
  this.getName = function(){
    return this.name
  }
}


// A $( document ).ready() block.
$( document ).ready(function() {

});
